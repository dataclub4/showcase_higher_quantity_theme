{% assign recycled_active = false %}
{% if settings.recycled_enabled %}
  {% assign bf_tags = settings.recycled_tags | replace: " ", ""  | split: "," %}
  {% if settings.recycled_icon and bf_tags %}
    {% for bf_tag in bf_tags %}
      {% if product.tags contains bf_tag %}
        {% assign recycled_active = true %}        
      {% endif %}
    {% endfor %}
  {% endif %}
{% endif %}

{% if recycled_active == true %}
      <div class="RecycledIcon {{ class }}" >
          <img src="{{ settings.recycled_icon | image_url }}">
        </div>

        <style>
        .product-block__inner, .product-area__media{
          position: relative;
        }
        .RecycledIcon{
          position: absolute;
          z-index: 2;
          display: flex;
          justify-content: center;
          align-items: center;
        }
      
        .RecycledIcon > img{
          height: 100%;
          width: 100%;
          object-fit: contain;
        }
      
        .product-block__inner .RecycledIcon{
          top: 10px;
          right: 10px;
          height: 50px;
          width: 50px;
        }
        
        .product-area__media .RecycledIcon{
          top: 50px;
          right: 10px;
          height: 80px;
          width: 80px;
        }
        
      </style>
{% endif %}